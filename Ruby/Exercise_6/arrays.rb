# frozen_string_literal: true

# This file is to practice arrays and some hashes.

arr_sq = %w['one', 'two', 'three', 4, #{3 * 5}]
arr_dq = %W['one', 'two', 'three', 4, #{3 * 5}]

puts(arr_sq)
puts(arr_dq)
